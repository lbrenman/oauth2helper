{
  "name": "OAuth2Helper",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:1-20-bullseye",
  "postCreateCommand": "npm install",
  "forwardPorts": [3000],
  "portsAttributes": {
    "3000": {
      "label": "OAuth2Helper",
      "onAutoForward": "notify"
    }
  },
  "postStartCommand": "echo 'Run: npm start (or npm run dev). When port 3000 is detected, use \"Open in Browser\" from the Ports panel (not the embedded preview) to open it in a real browser tab. Copy settings.example.json to settings.json to get started.'"
}
